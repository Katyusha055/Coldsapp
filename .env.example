DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=

# development (default) or production. Must be exactly one of those two.
ENV=

SECRET_KEY=
ALGORITHM=
ACCESS_TOKEN_EXPIRE_HOURS=

EVO_API_URL=
EVO_API_TOKEN=
WHATSAPP_WEBHOOK_URL=

# Comma-separated list of allowed CORS origins, e.g. http://localhost:5173,https://your-frontend-domain
CORS_ORIGINS=

# Base URL the frontend uses to reach the API only when running locally in docker-compose. 
VITE_API_URL=